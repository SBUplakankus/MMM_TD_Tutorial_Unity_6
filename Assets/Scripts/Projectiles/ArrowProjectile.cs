using Interfaces;
using UnityEngine;

namespace Projectiles
{
    public class ArrowProjectile : ProjectileBase
    {
        // TODO: Episode 04 — Arrow variant: straight-line projectile (no homing after direction set)
        // Override Launch to set direction, override Move to fly straight
        // Override OnHit to decrement pierce count, return to pool when exhausted
    }
}