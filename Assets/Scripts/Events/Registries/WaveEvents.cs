using Events;

namespace Events.Registries
{
    public class WaveEvents
    {
        // TODO: Episode 11 — WaveStarted (EventChannel<int>), WaveCompleted (EventChannel), AllWavesCompleted (EventChannel)
    }
}